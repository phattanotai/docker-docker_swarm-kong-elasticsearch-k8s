apiVersion: apps/v1
kind: Deployment
metadata:
  name: trn-master-data-deployment
spec:
  selector:
    matchLabels:
      app: trn-master-data
  replicas: 2
  template:
    metadata:
      labels:
        app: trn-master-data
        env: development
    spec:
      containers:
      - name: trn-master-data
        image: asia.gcr.io/platinum-honor-326814/trn-master-data:v1
        ports:
        - containerPort: 30000
        env:
        - name: NODE_ENV
          value: "production"
        - name: DB_HOST
          value: "trn_mongodb"
        - name: DB_PORT
          value: "27017"
        - name: DB_USERNAME
          value: "admin"
        - name: DB_PASSWORD
          value: "1234"
        - name: DB_DATABASE
          value: "trn"
---
apiVersion: v1
kind: Service
metadata:
  name: trn-master-data-service
spec:
  type: ClusterIP
  ports:
  - port: 30000
    targetPort: 30000
    protocol: TCP
  selector:
    app: trn-master-data